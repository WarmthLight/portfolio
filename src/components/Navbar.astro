---
import { site } from '../data/site';
import { ui } from '../data/i18n';
import LanguageSwitcher from './LanguageSwitcher.astro';

const { lang } = Astro.props;
const t = ui[lang];
const base = import.meta.env.BASE_URL;
const currentPath = Astro.url.pathname;

const navLinks = [
  { href: `${base}${lang}/unity`, label: t.nav.unity },
  { href: `${base}${lang}/uiux`, label: t.nav.uiux },
  { href: `${base}${lang}/aigc`, label: t.nav.aigc },
  { href: `${base}${lang}/about`, label: t.nav.about },
];
---

<nav class="fixed top-0 left-0 right-0 z-50 glass-nav" id="navbar">
  <div class="px-6 sm:px-8 lg:px-16 h-16 flex items-center justify-between">
    <a href={`${base}${lang}/`} class="flex items-center gap-2.5 group flex-shrink-0">
      <div class="w-8 h-8 rounded-lg bg-gradient-to-br from-[var(--color-primary)] to-[var(--color-accent)] flex items-center justify-center shadow-sm group-hover:shadow-md transition-shadow">
        <span class="text-white text-sm font-bold">P</span>
      </div>
      <span class="text-lg font-semibold text-[var(--color-text)] group-hover:text-[var(--color-primary)] transition-colors hidden sm:inline">
        {site.name[lang]}
      </span>
    </a>
    <div class="hidden md:flex items-center gap-0.5">
      {navLinks.map(link => {
        const isActive = currentPath.includes(link.href);
        return (
          <a
            href={link.href}
            class={`relative px-4 py-2 rounded-lg text-sm transition-all duration-200 ${
              isActive
                ? 'text-[var(--color-primary)] font-medium'
                : 'text-[var(--color-text-muted)] hover:text-[var(--color-text)] hover:bg-[var(--color-bg-alt)]'
            }`}
          >
            {link.label}
            {isActive && (
              <span class="absolute bottom-0.5 left-1/2 -translate-x-1/2 w-4 h-0.5 rounded-full bg-[var(--color-primary)]"></span>
            )}
          </a>
        );
      })}
      <div class="w-px h-5 bg-[var(--color-border)] mx-3"></div>
      <LanguageSwitcher lang={lang} currentPath={currentPath} />
    </div>
    <button
      id="menu-toggle"
      class="md:hidden flex items-center justify-center w-10 h-10 rounded-lg hover:bg-[var(--color-bg-alt)] transition-colors"
      aria-label="Toggle menu"
    >
      <svg id="menu-open" class="w-5 h-5 text-[var(--color-text)]" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16"/>
      </svg>
      <svg id="menu-close" class="w-5 h-5 text-[var(--color-text)] hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/>
      </svg>
    </button>
  </div>
  <div id="mobile-menu" class="hidden md:hidden border-t border-[var(--color-border-light)] bg-white/95 backdrop-blur-xl">
    <div class="px-4 py-4 space-y-1">
      {navLinks.map(link => (
        <a
          href={link.href}
          class={`block px-4 py-2.5 rounded-lg text-sm transition-all ${
            currentPath.includes(link.href)
              ? 'text-[var(--color-primary)] bg-[var(--color-primary-50)] font-medium'
              : 'text-[var(--color-text-muted)] hover:text-[var(--color-text)] hover:bg-[var(--color-bg-alt)]'
          }`}
        >
          {link.label}
        </a>
      ))}
      <div class="px-4 pt-2">
        <LanguageSwitcher lang={lang} currentPath={currentPath} />
      </div>
    </div>
  </div>
</nav>

<script>
  const toggle = document.getElementById('menu-toggle');
  const menu = document.getElementById('mobile-menu');
  const openIcon = document.getElementById('menu-open');
  const closeIcon = document.getElementById('menu-close');

  toggle?.addEventListener('click', () => {
    menu?.classList.toggle('hidden');
    openIcon?.classList.toggle('hidden');
    closeIcon?.classList.toggle('hidden');
  });

  menu?.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', () => {
      menu?.classList.add('hidden');
      openIcon?.classList.remove('hidden');
      closeIcon?.classList.add('hidden');
    });
  });
</script>
