---
import Base from '../../layouts/Base.astro';
import ProjectCard from '../../components/ProjectCard.astro';
import { getCollection } from 'astro:content';
import { ui } from '../../data/i18n';

const lang = 'zh';
const t = ui[lang];
const projects = (await getCollection('projects')).sort((a, b) => a.data.order - b.data.order);
---

<Base lang={lang} title="Unity 开发 - 作品集">
  <section class="px-6 sm:px-8 lg:px-16 pt-24 sm:pt-32 pb-16 sm:pb-24">
    <div class="mb-12 text-center">
      <p class="text-sm font-medium text-[var(--color-primary)] tracking-wide uppercase mb-3">{t.unity.label}</p>
      <h1 class="text-4xl font-bold text-[var(--color-text)]">Unity 游戏开发</h1>
    </div>
    {projects.length > 0 ? (
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">
        {projects.map(project => (
          <ProjectCard project={project} lang={lang} basePath="/zh/unity" />
        ))}
      </div>
    ) : (
      <div class="text-center py-24 rounded-2xl border border-dashed border-[var(--color-border)]">
        <div class="text-5xl mb-4">🎮</div>
        <p class="text-[var(--color-text-muted)] text-lg">{t.unity.empty}</p>
      </div>
    )}
  </section>
</Base>
